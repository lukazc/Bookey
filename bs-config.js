// lite-server config
// currently not used

module.exports = {
    server: { baseDir: './src'},
    ghostMode: false,
    reloadDelay: 1000,
    reloadDebounce: 1000,
    injectChanges: false,
    minify: false,
}
